<aside class="contact-cta" aria-labelledby="contact-cta-title">
  <div>
    <p class="eyebrow">Open channel</p>
    <h2 id="contact-cta-title">Have a systems problem worth discussing?</h2>
    <p>
      I’m interested in ML systems, infrastructure, and engineering work with visible boundaries.
    </p>
  </div>
  <div class="contact-cta__actions">
    <a class="button-link" href="mailto:abdulrahman.gomaa.h05@gmail.com">Send an email</a>
    <a
      class="button-link button-link--secondary"
      href="https://www.linkedin.com/in/abdulrahman-gomaa">LinkedIn</a
    >
  </div>
</aside>

<style>
  .contact-cta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    padding: var(--space-5);
    border: 1px solid var(--color-border-strong);
    background: var(--color-surface);
  }

  .contact-cta > div {
    max-width: 42rem;
  }

  .contact-cta h2 {
    margin: var(--space-1) 0;
    font-size: clamp(1.65rem, 4vw, 2.5rem);
    line-height: 1.12;
  }

  .contact-cta div > p:last-child {
    margin: 0;
    color: var(--color-text-muted);
  }

  .contact-cta__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }
</style>
